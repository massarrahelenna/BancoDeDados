CREATE TABLE produtos (
    id INT PRIMARY KEY,
    nome VARCHAR(100),
    preco DECIMAL(10,2)
);

-- TABELA alunos :
CREATE TABLE alunos (
    id INT PRIMARY KEY,
    nome VARCHAR (400),
    materia VARCHAR (100),
    nota INT
);